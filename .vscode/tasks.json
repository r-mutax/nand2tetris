{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build assembler",
            "type": "shell",
            "command": "make",
            "problemMatcher": [],
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/06/assembler"
            }
        }
    ]
}